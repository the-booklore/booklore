stages:
  - build
  - test
  - package-build
  - package-test
  - publish
  - mirror

workflow:
  rules:
    # scheduled pipeline: only mirror job should run
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: always

    # tag pipeline (e.g. semantic-release created a tag)
    - if: '$CI_COMMIT_TAG'
      when: always

    # merge request pipelines
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: always

    # normal branch pushes
    - if: '$CI_PIPELINE_SOURCE == "push"'
      when: always

    - when: never


include:
  # semantic-release (only meaningful on main branch pushes)
  - component: "$CI_SERVER_FQDN/to-be-continuous/semantic-release/gitlab-ci-semrel@4.0"
    inputs:
      changelog-enabled: true
      auto-release-enabled: true

  # docker template
  - component: "$CI_SERVER_FQDN/to-be-continuous/docker/gitlab-ci-docker@8.0"
    inputs:
      trivy-disabled: true
      hadolint-disabled: true
      prod-publish-strategy: "none"
      push-args: "--compression-format zstd --compression-level 20"
      # default snapshot tag (we override for MR via rules below)
      snapshot-image: "$CI_REGISTRY_IMAGE/snapshot:$CI_COMMIT_REF_SLUG"


# -----------------------------
# Tests (run on pushes + MRs)
# -----------------------------
backend_tests:
  stage: test
  image: eclipse-temurin:25-jdk
  tags: [universal]
  script:
    - cd booklore-api
    - ./gradlew test --no-daemon --parallel
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_COMMIT_TAG'
      when: never
    - when: on_success

frontend_tests:
  stage: test
  image: node:22
  tags: [universal]
  script:
    - cd booklore-ui
    - npm install --force --legacy-peer-deps
    - npx ng test
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: '$CI_COMMIT_TAG'
      when: never
    - when: on_success


# -----------------------------
# Mirror (schedule only)
# -----------------------------
mirror_from_github:
  stage: mirror
  image: alpine:latest
  tags: [universal]
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: on_success
    - when: never
  before_script:
    - apk add --no-cache git
  script:
    - git clone --branch develop --single-branch https://github.com/booklore-app/booklore.git source
    - cd source
    - git remote add gitlab https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.theprivateserver.de/WorldTeacher/wt-booklore.git
    - git push --force gitlab develop
    - git push --force gitlab --tags


# -----------------------------
# to-be-continuous overrides
# -----------------------------

# Make semantic-release run ONLY on main branch pushes (not MRs, not tags, not schedule)
.semrel-base:
  tags: [universal]
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
      when: on_success
    - when: never
  variables:
    # ensure semrel only considers main (align with your "master should die" repo model)
    SEMREL_BRANCHES_REF: "^main$"

# Make ALL docker component jobs run ONLY on:
# - merge_request_event (MR snapshot build)
# - tag pipelines (release image build/publish)
.docker-base:
  tags: [buildah]
  rules:
    # MR: build/push an MR snapshot image AFTER tests (stage ordering ensures tests must pass)
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success
      variables:
        # Put MR images somewhere obvious; adjust naming to taste
        DOCKER_SNAPSHOT_IMAGE: "$CI_REGISTRY_IMAGE/mr:$CI_MERGE_REQUEST_IID"
        DOCKER_BUILD_ARGS: >-
          --platform linux/amd64
          --platform linux/arm64
          --build-arg APP_VERSION=$CI_MERGE_REQUEST_IID
          --build-arg APP_REVISION=$CI_COMMIT_SHA

    # Tag: build/publish the release image with the tag
    - if: '$CI_COMMIT_TAG'
      when: on_success
      variables:
        DOCKER_BUILD_ARGS: >-
          --platform linux/amd64
          --platform linux/arm64
          --build-arg APP_VERSION=$CI_COMMIT_TAG
          --build-arg APP_REVISION=$CI_COMMIT_SHA

    - when: never
